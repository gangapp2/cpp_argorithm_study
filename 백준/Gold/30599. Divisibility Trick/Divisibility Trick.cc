#include<iostream>
#include<vector>
#include<algorithm>
#include <string>
using namespace std;

int main() {
	string s;
	int N;
	cin >> N;
	to_string(N);
	for (int i = 0; i < N; ++i) {
		cout << N;
	}
	return 0;
}